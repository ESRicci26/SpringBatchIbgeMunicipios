# SpringBatchIbgeMunicipios
Aplicação Spring Batch (Maven, Java 11 usando todo fluxo da arquitetura padrão do Spring Batch: "Job" → "Step" → ("Tasklet" | "ItemReader"/"ItemProcessor"/"ItemWriter"), "JobRepository", "JobLauncher", execução em chunks e listeners.
